import { Metadata } from "next";
import UI from "./ui";

export const metadata: Metadata = {
  title: "로그아웃 - 인프런",
  description: "인프런은 로그아웃 페이지입니다.",
};
export default function SignupPage() {
  return <UI />;
}
